{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_new form-signin">
    <h2 class="form-signin-heading">{{ 'group.new.submit'|trans }}</h2>
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-large btn-primary" value="{{ 'group.new.submit'|trans }}" />
    </div>
</form>
