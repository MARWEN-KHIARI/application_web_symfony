{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit form-signin">
    <h2 class="form-signin-heading">{{ 'profile.edit.submit'|trans }}</h2>
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-large btn-primary" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
</form>
